---
clip: "028"
file: "28-Engineering- 2026-09-15.mp4"
day: "2026-09-15"
module: engineering
duration: "00:09:59"
speakers: ["Matt", "Lauren"]
status: aligned
---

## 00:00:00–00:01:00  [talking-head]
**Spoken:** (continues from 027) Matt: "On the design, I have some skills I use — 'make design / make interfaces feel better' is a good one; I prefer minimalism, and that's maybe a good place to start before we start working on some branding. So I'm going to take that prototype you built, Lauren, and turn it into something I think looks nice and maybe a little bit more professional, while you kind of work on the backend services — and then we can tie in some waitlist sign-up type stuff." Lauren (on her screen): "It spawned a cloud agent, and I guess we're just waiting for it to build the pstack plugin (ASR 'PSAC plugin') for me. While we wait, we can take a quick look at PStack and what that skill does." Two parts of a verification skill: (1) "a CLI, or a script, or some kind of tool that allows the bots or agents to reliably and deterministically interact with your application. Bots and agents are pretty smart now — they can just write scripts on the fly — but that obviously uses a lot of tokens, and it's also not reproducible, because now every bot has its own script for doing the verification. I'd rather it just have a standard set of tools. So the verification skill is going to create this CLI for us." (2) "It's also going to create something called a **feature map** — not super important for us right now because we don't really have any features. The idea of the feature map is it's a map for the agents and the bots to understand the application: what features you have, how to get to it, keyboard shortcuts, and so on."
**On screen:** Matt's Grok Bot (Founding Eng): lander task options "A Type/spacing/scale, B Logo treatment, C OG image too, D I'll describe it… Going to tighten type, spacing, and scale on the current title-only lander, same Geist / timer / black-on-white. No new copy." steve's thread: "handoff: generate .cursor/skills/verify-<app>/ for thursday, prove the waitlist signup path once, open a PR, post it in #pr-reviews. #bug-reports (CQC2XH3SEAC) is logged as the triage channel. admin dashboard next: grokpot for a throwaway mock after verification can actually run the app. hold grokpot until then…" tater: "checking tater now — last I had was verification skill in flight… verification skill is in a cloud agent — no PR yet." PlanetScale Bot: "done moving."
**Actions:** Matt takes the lander design; Lauren walks the verification-skill concept while her cloud agent builds it.
**Facts:**
- Matt's design approach: Grok Bot skills like "make design / make interfaces feel better"; minimalism first, branding later
- Verification skill = two parts: a deterministic CLI/tool for bots to drive the app (on-the-fly scripts cost tokens and aren't reproducible) + a feature map (what features exist, how to reach them, shortcuts)
- steve's handoff for the thursday repo: generate `.cursor/skills/verify-<app>/`, prove the waitlist signup path once, open a PR, post it in #pr-reviews
- #bug-reports channel is registered as the triage channel (channel id on screen)
- A PlanetScale Bot exists in the sidebar ("done moving" — DB setup)
- pstack PR #5 already merged; open leftovers: #2, #6, #7, #8 (lander/prototypes — not the current job)

## 00:01:00–00:03:15  [ui-demo]
**Spoken:** Lauren (reading the pstack docs live): the create-verification-skill docs; the feature-map example (a Notes app) shows the shape: launch isolated with a disposable data dir, a `doctor` check, drive via a control CLI (browser/CLI), proof conventions (ARIA snapshots + screenshots with app identity; CLI proof = command + stdout/stderr + exit code; mutation proof = read-only second view), and a feature-entry contract (H1 + four H2s: Sub-features / How to get to it (user POV) / Driving it with <harness> / Gotchas). Cloud agent live on the task "verify-thursday skill + signup E2E" (repo shipbythursday/thursday): "TASK from Lauren via Steve (tater coordinating). Project coordinator could not spawn a worker — you own this end-to-end. Environment ready. I'm routing this through the repository's required poteto workflow. It will interview the codebase and attached recipe, then implement and prove the verification skill from current main… Build waitlist verification skill — Work end-to-end in /workspace. Read poteto-mode SKILL.md in full… Start from current remote main… create branch exactly cursor/verify-thursday-1f82." Matt (waitlist): "I'm working on the waitlist right now. We really just need a way for people to submit emails and then log that somewhere. There are a couple of different platforms — Loops (ASR) for this, which is actually a really great platform; you can also use Resend. I might just start with Resend, because it's pretty simple to get started and agents are quite good there… We might have a plugin for it — I think we do." Later: "By the way, the domain is live — it's now connected." Matt: "Do we have any submissions yet?" Lauren: "I hooked it up to our database… if there's anyone hitting the page, I think we'll start seeing entries." Matt: "We already have a form submission! I didn't even know… We actually have a database hooked up." Lauren: "But I think we want to redesign the form first — right now it's kind of live-coded… I don't know if we're even collecting information that's useful. So if you're submitting, maybe don't submit yet, because it's just a throwaway prototype for now."
**On screen:** GitHub: cursor/plugins/pstack (Lauren's open-source plugin; 42 issues, 84 PRs) — skills list: architect, arena, automate-me, blast-radius, bro, create-verification-skill, figure-it-out, how, interrogate, maintain-verification-skill, no-comments, poteto-mode, pr-review-canvas. create-verification-skill SKILL.md: "Generate a project-local verification skill that drives your app the way a user does… .cursor/skills/verify-<app>/… 1. Interview the repo, not the user (Surface / Run / Drive / Observe / Isolate)… 3. Seed the feature map… 4. Prove the generated skill before handing it over… 5. Offer the maintenance loop (/maintain-verification-skill)." Google: flaviocopes.com "A deep dive into pstack — pstack is a Cursor plugin for rigorous agent work… /poteto-mode: Acts as a central command router that selects the correct playbook, logs task lists, and enforces runtime verification." X: Zack Jackson: "@poteto pstack plugin is so fantastic. I was inspired by /automate-me and wrote /automate-maintainer…" Cursor cloud agent UI + PlanetScale "thursday dashboard".
**Actions:** Lauren shows the pstack repo/docs while the cloud agent builds the thursday verification skill; Matt picks Resend for waitlist emails; the team discovers a live form submission already in the PlanetScale DB.
**Facts:**
- pstack is open source at github.com/cursor/plugins (pstack); skills include create-verification-skill, maintain-verification-skill, poteto-mode, pr-review-canvas, automate-me, bro, architect, arena, blast-radius, figure-it-out, how, interrogate, no-comments
- create-verification-skill contract: interview the repo (Surface/Run/Drive/Observe/Isolate), generate .cursor/skills/verify-<app>/ with helpers + feature map, prove it end-to-end once (a never-executed skill is a draft), point to /maintain-verification-skill
- Feature-map contract: H1 + four H2s (Sub-features, How to get to it (user POV), Driving it with <harness>, Gotchas); proof rules (ARIA snapshot + screenshot with app identity; command+stdout+exit code; read-only second view for mutations)
- The thursday cloud agent runs the repo's poteto workflow, branches cursor/verify-thursday-1f82, works in /workspace
- Waitlist email choices: Loops or Resend; Matt starts with Resend (simple; agents are good there; a plugin exists)
- The domain is live and connected; the waitlist form already posts into the PlanetScale database and received at least one submission
- Form is a throwaway prototype — audience/questions will be redesigned before collecting

## 00:03:15–00:05:30  [ui-demo]
**Spoken:** Matt (to Founding Eng, dictating): "I'm going to do a little rant with what I think we need: we need to redesign the UI to be much more minimalist first of all… second, we want to orient around collecting guests, or like reservations. So what I'm thinking is we just want an email sign-up for people that want to join our pop-up. Once the person signs up, we're going to log them in the database. In the top-right corner we want a button for restaurateurs or other proprietors, or people who can help us make this happen. So primarily we collect guest booking information… secondarily, people that want to submit a request to help us out, because we need some help. We're going to need to go find some people… I think we're just going to have to send a lot of emails to do market research… I'm going to work on that personally, but I want this bot to redesign the website."
**On screen:** Cloud agent progress: "App starts cleanly. Next I'll draft the skill and helpers from the interview, then prove signup end to end… $ Install Playwright Chromium browser… Creating the skill, helpers, and feature map from the interview findings." Generated artifacts: `launch.sh` (isolated PLATFORM waitlist instance; VERIFY_PORT 3010; PGlite DATA_DIR), `doctor.sh` (read-only health check), `cleanup.sh` (never deletes proof), `drive-join-waitlist.mjs` (Playwright E2E), SKILL.md `name: verify-platform-waitlist` ("Drive the PLATFORM operator waitlist (night-market pink landing + POST /api/signup) the way a user does… duplicate handling, or email validation against PGlite"), feature map files join-waitlist / choose-role (role-press, role-exclusive) / duplicate-signup (HTTP 409 on repeat POST) / invalid-email. Then: "Committing and pushing the skill implementation before the E2E proof run… $ Run full E2E verify join-waitlist flow… Fixing the Playwright import and cleaning up the failed run… End-to-end verification succeeded." Then: "PR creation is blocked. The GitHub token lacks permission to create pull requests. I am checking for undocumented tools or alternative authentication methods…"
**Actions:** Matt dictates the waitlist redesign brief to Founding Eng; the cloud agent builds, proves, and commits the verification skill, then hits a GitHub permission wall on PR creation.
**Facts:**
- Waitlist v1 brief: minimalist UI; primary = guest email signup (logged to DB); secondary = top-right button for restaurateurs/proprietors/helpers; Matt does the market-research emails personally
- Generated verification artifacts: launch/doctor/cleanup scripts + Playwright driver + feature map (join-waitlist, choose-role, duplicate-signup, invalid-email); proofs against PGlite
- Skill self-proof: the agent ran the full E2E join-waitlist flow successfully and captured screenshots as evidence
- Failure surfaced honestly: "PR creation is blocked. The GitHub token lacks permission to create pull requests" — the agent then hunted for alternative auth/tools (ManagePullRequest via CallDynamicTool)

## 00:05:30–00:07:54  [talking-head]
**Spoken:** Lauren (watching tater's cloud agent): "Is the cloud agent stuck? Let's see why this is taking so long." steve self-checks: "checking whether it's actually moving or just sitting there… oh nvm, it's using a subagent… yep. still chasing the PR, not the spinner." Lauren: "While we wait for the verification skill, ask @tater to use Poteto Mode (via a cloud agent) to build out some prototypes of features we'll need in our platform — for example — and help me brainstorm what we'll need."
**On screen:** steve thread (full state): "tater's moving. verification skill is in a cloud agent — no PR yet… running: verify-thursday skill + signup E2E… done looks like: .cursor/skills/verify-* in a PR, signup path proven once, link in #pr-reviews. side note: pstack PR #5 already merged. leftover open: #2, #6, #7, #8…" Slack #bug-reports: lauren joined; "Matt Palmer was added to #bug-reports by lauren. Also, roshan and Cursor joined." #new-channel created by roshan. Connected plugins bar: **Vercel connected Plugin · PlanetScale connected Plugin · Slack connected Plugin**.
**Actions:** Lauren nudges steve/tater; steve diagnoses the cloud agent ("using a subagent"), and Lauren queues the platform-prototype brainstorm via Poteto Mode.
**Facts:**
- steve monitors cloud agents and self-diagnoses stalls ("is the cloud agent stuck?… oh nvm it's using a subagent")
- Next work queued: tater to use Poteto Mode via a cloud agent to prototype platform features and brainstorm requirements
- Plugins connected for the company team: Vercel, PlanetScale, Slack
- Slack workspace state: #bug-reports (default channel; Cursor joined), #new-channel (roshan), #pr-reviews

## 00:07:54–00:10:00  [ui-demo]
**Spoken:** Matt + Lauren brainstorm the platform model: "What features will the platform actually have? We have a sign-up page… login screen… what do people do on this platform? You want to host — you want to create a pop-up. What's the first thing you need? A name for your pop-up… a date and time… you want submissions and then you want the event. So the first thing is to create an event page — the defining thing would be an event, and an event has a bunch of other related tables. People need an invitation, a way to collect sign-ups for their pop-up. We will need tools for managing staff… permissions on the event itself — maybe there's an event owner, and then hosts, and attendees. If we have a static account type across the entire platform, we could split it out that way." Matt: "I feel like I need a whiteboard — maybe in TLDraw." Also: "Do we help our users do market research as well? If we had maps, or a database where we have location data, you can start thinking about places you want to host your pop-up." Matt to the panel: "Do you like to try and think about bigger features like this, or is it more of a scrappier MVP approach (ASR 'tracer kind of MVP')?"
**On screen:** Matt types to steve: "…user should be able to create their own event. events have a date/time, signups, venue, staff…" grokpot: "baking admin.html." tater: "tater posted #10 to #pr-reviews. automation actually fired this time — fable review is running: https://cursor.com/agents/bc-a0749c23-e113-5b4b-921f-7562041e243d" — PR #10 in #pr-reviews, the review automation ("fable") fired. steve: "Verify skill landed as… 4 messages with 3 agents." Live site visible: popup-shipbythursday.vercel.app — "PLATFORM matches chefs, hosts with space, and food pros so you can launch a food pop-up in your city — find a location, find operators, skip the permanent stack." (HOST/LOCATION · FOOD PRO · OPERATOR).
**Actions:** Matt and Lauren whiteboard the event-centric data model in chat and TLDraw; grokpot starts an admin mock; tater's PR #10 lands in #pr-reviews and the review automation fires.
**Facts:**
- Platform data model brainstorm: **event-centric** — create event (name, date/time, venue) with related tables: signups/invitations, staff/permissions (owner, hosts, attendees as static account types), list of events; venue/maps/location data; possibly market-research help for users
- Open product question: big-feature thinking vs scrappy MVP
- grokpot begins an admin dashboard mock (admin.html)
- tater posted PR #10 to #pr-reviews and the review automation ("fable", the previously-untitled one) actually fired this time
- Live landing: popup-shipbythursday.vercel.app — operator-waitlist copy for chefs/hosts/food pros/operators
