---
clip: "075"
file: "11-Sales Engineering- 2026-09-16.mp4"
day: "2026-09-16"
module: sales-engineering
duration: "00:09:59"
speakers: ["Lauren", "Host"]
status: aligned
---

## 00:00:00–00:01:30  [ui-demo]
**Spoken:** (continues from 074 — Lauren reading the game spec back to check it "makes sense") Lauren reads the bot's "Restate Cupcake game" summary aloud: a short competitive loop — a player logs in, imports one x.ai bot, completes a three-bot roster, the lineup is hidden from the opponent, you match another account, and get a three-round result plus a replay and an Elo change. "Good, it caught the Elo part, which is really important." She has never designed a matchmaking system; she's played games like Marvel Rivals that have papers published, so she'll use that as a reference and keep it simple, then iterate. Elo is a score almost like a chess rating: the more games you win the score goes up, but there's a factor — depending on who you're matched against, the score rises by a smaller or bigger amount. Then there's "a bunch of sign-in stuff" she wants to skip for the prototype.
**On screen:** Grok Bot chat — "Game documentation summary" + bot task "Restate Cupcake game". Summary text: "Cupcake is a planned 1v1 bot-battle game. You sign in with X, bring one public bot, get two more from a shared pool, set the order in secret, then play three rounds. The first mint of each bot is permanent and the same for every player." Rules doc: "A player logs in, imports one x.ai bot, completes a three-character roster, hides the lineup, matches another account, and gets a three-round result plus a replay and Elo change." "New accounts start at 1000. k is 32." Rarity: Common 90% (cap 100), Uncommon 8% (110), Rare 1% (120), Epic 0.9% (130), Legendary 0.1% (150). Combat: "CHA beats INT, INT beats DEX, DEX beats CHA. Only the advantaged side is multiplied by 1.22" (integer scale factors 122/100). "The repo is still only README.md and that plan. There is no game code yet."
**Actions:** Lauren reads the summary back line by line; the host confirms the Elo/matchmaking point.
**Facts:**
- Game code name: **Cupcake** — a planned 1v1 bot-battle ("three-bot strategy") game, explicitly not an auto-battler with shops/progression
- Core loop: sign in with X → import one x.ai bot → +2 random pool characters → set order in secret → match another account → three rounds → replay + Elo change
- Elo: new accounts start at 1000, K=32; wins raise the score, magnitude depends on the opponent's strength
- Rarity drawn once per template: Common 90% (cap 100) / Uncommon 8% (110) / Rare 1% (120) / Epic 0.9% (130) / Legendary 0.1% (150)
- Combat cycle: CHA beats INT, INT beats DEX, DEX beats CHA; only the advantaged side ×1.22 (integer scale factors 122/100)
- First mint is permanent and shared — importing the same template yields the identical character; no remint
- Matchmaking reference for Lauren (her first system): Marvel Rivals' published papers

## 00:01:30–00:03:10  [ui-demo]
**Spoken:** Lauren types the prototype brief into Grok Bot: skip sign-in for now, just focus on the game loop so they can iterate on it quickly; "use /poteto-mode prototyping — I want to have a few different prototypes to choose from"; keep everything local for now, no DB required; include debug panels with sliders and input boxes to tweak game variables and constants for a quick iteration loop. Host: "You have a prototyping section of potato mode, right? That's how the agent knows what to do there." Lauren: the point of a game is that it's fun — all the other stuff doesn't matter if the game isn't fun — so cut it down to just the core loop. Seed some bots from x.ai/bots/marketplace as the default set. The sliders let her tweak things in real time and replay to see "is this actually fun"; if not, think about how to make it fun. Host: that gives a playground to tweak the MVP live instead of prompting back-and-forth, iterating much quicker.
**On screen:** Grok Bot composer with the typed instructions: "for the prototype, lets skip sign in for now - just focus on the game loop so we can iterate on it quickly"; "use /poteto-mode prototyping - i want to have a few different prototypes to choose from"; "lets keep everything local for now, no db required"; "include some debug panels where i have sliders and input boxes ... and constants for the quick iteration loop"; "seed some bots from x.ai/bots/marketplace". Sidebar skills list (poteto-mode, principle-experience-first, architect, etc.).
**Actions:** Lauren writes the four-part prototype brief; the agent begins reading the prototype workflow and its design rules.
**Facts:**
- Prototype scope: no sign-in, no DB, all-local, single core game loop — "the point of the game is that it's fun"
- Debug panels (sliders + input boxes) requested so game variables/constants can be tuned live and replayed without re-prompting
- Seed data: bots from x.ai/bots/marketplace as the default set
- poteto-mode invoked via "/poteto-mode prototyping" — a plugin for Grok Bot and Cursor that drives software builds

## 00:03:10–00:05:10  [ui-demo]
**Spoken:** Lauren explains potato mode: it's a "router skill" — not many specific instructions itself, but for different scenarios "use these skills and these principles." In "PSTAC" (ASR) there are principles — e.g. Experience First: choose user delight over implementation convenience; every feature must be justified; ship less but ship better; prototype before committing (design decisions are cheaper in throwaway HTML than production code). The agent is iterating on a simple in-memory object with seeded bots and a three-slot lineup. Potato mode also has an "Architect" skill that uses different models to produce candidate plans — here four models: Fable, Sol, Grok, and Composer — "erase [race] them and see who comes up with the better architecture," then pick the best and merge interesting ideas from the others (different models excel at different tasks; they surface constraints/criteria you may not have thought of).
**On screen:** "Experience First" SKILL.md: "When implementation convenience conflicts with user delight, choose delight. Every feature, control, and option must be justified. Ship less, ship better (polished experience with three features beats rough one with ten). Prototype before committing (design decisions are cheaper in throwaway HTML than production code). Get the details right (transitions, alignment, spacing, feedback, error states). Tighten the core loop… The user is whoever consumes the work." "Sketch prototype architecture A / B / C / D" tasks visible.
**Actions:** Lauren narrates potato mode internals while the agent plans and spawns architecture candidates.
**Facts:**
- potato mode = a router skill: it routes scenarios to specific skills/principles rather than holding detailed instructions itself
- Principles live under "PSTAC" (ASR); Experience First = delight > implementation convenience, justify every feature, ship less/ship better, prototype in throwaway HTML
- Architect skill = multi-model architecture bakeoff: Fable, Sol, Grok, Composer each draft a plan, then cross-judge and merge the best ideas
- Prototype data model: one in-memory object + seeded bots + three-slot lineup

## 00:05:10–00:07:00  [ui-demo]
**Spoken:** Lauren decides to skip the architect bakeoff (slower, and she doesn't care about architecture for a prototype) and use Grok for the first quick prototype, run locally so they can actually play it. Host (mid-stream recap): "for those joining — we're in the middle of our three-day sprint to ship and launch a real product… a game studio with our first game being built live. You're watching Lauren demonstrate potato mode, a plugin available for Grok Bot and Cursor… Solstuff (ASR) you can go and use on your own." Lauren notes Cursor has an inbuilt browser ("I have Subway Surfers in here" — a favorite for waiting on an agent). Grok completes a design: the throwaway prototype is vanilla HTML, CSS, and JavaScript, everything kept in memory, three UI variants to toggle between, fetching marketplace bot names as mock seed data.
**On screen:** Cursor "Game documentation summary" + "design.md" + "Subway Surfers Web" tab; "Cupcake throwaway prototype — candidate 3"; "Building a single-file local prototype with three layouts, a shared in-memory match model, and a debug dock." "Vanilla HTML, CSS, and JavaScript." Prompt: "let skip the architect skill for now - lets just use grok for the first quick prototype. i want to run it locally in your @Browser so we can click around and play it."
**Actions:** Lauren types the skip-architect + use-Grok instruction; the agent writes design.md and begins the single-file prototype.
**Facts:**
- Architect bakeoff skipped for the prototype (speed) — Grok chosen for the first pass
- Throwaway prototype stack: single-file vanilla HTML/CSS/JS, in-memory state, three UI variants behind a switcher
- Cursor's inbuilt browser used to run the prototype locally
- Mid-stream recap: three-day sprint to launch a real product; the company is a "game studio" building its first game live; potato mode is a Grok Bot + Cursor plugin

## 00:07:00–00:09:59  [ui-demo]
**Spoken:** Lauren walks the generated design: marketplace names are display-only (copied from the current x.ai marketplace list) with no stable template IDs or template JSON in the fetched text, so every identifier and combat field is labeled "mock"; the shared game model stays small while the three variants stay meaningfully different; then data shapes/types and state transitions (factory bots + default settings → draft → fight → frozen replay). "This code is not very interesting because it's just vanilla… but it's kind of data-driven." (continues on 076)
**On screen:** design.md typedefs: Stat ("CHA"|"INT"|"DEX"), Rarity ("common"|"uncommon"|"rare"|"epic"|"legendary"), VariantId ("rank-reveal"|"slot-ledger"|"cycle-tape"); MARKETPLACE_DISPLAY_NAMES list ("dr eggbot", "Overheard", "Tradbot", "Projects Manager", "Researchy", "tinkabot", "Nightly Audit Engineer", "Outbound Prospecting", "Flora", "…Critiquito"); "State transitions" section.
**Actions:** Lauren reviews the design doc top to bottom while the agent keeps implementing.
**Facts:**
- x.ai marketplace page exposes no stable template IDs or template JSON → all IDs and combat fields must be labeled "mock"
- Marketplace display names (Dr. Eggbot, Overheard, Tradbot, Projects Manager, Researchy, tinkabot, Nightly Audit Engineer, Outbound Prospecting, Flora, Critiquito) used as seed data
- Three UI variants: rank-reveal, slot-ledger, cycle-tape
- Domain stats CHA/INT/DEX and rarities common/uncommon/rare/epic/legendary defined in the type sketch
